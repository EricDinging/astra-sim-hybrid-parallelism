# Generated trace library (per-rank Chakra ET files + stage metadata). Never
# committed -- regenerate with ./reproduce.py prereq.
/tracelib/
*.et

# Measured per-shape service-time table (output of measure_svc.py) and any
# generated arrival-process run dirs. Reproduce, don't commit.
/service_times.csv
*service_times*.csv
/runs/

# Python bytecode + tool caches.
__pycache__/
*.pyc
.ruff_cache/

# Simulation worker servers.
workers.txt

# BW/LT schedule matrices: generated artifacts (torus and fullmesh fully
# determined by cluster.json dims + network.yml bw/latency). Rebuilt by
# ./reproduce.py prereq; the *_fullmesh.txt pair feeds the `ideal` placement.
/configs/bandwidth_schedule_*.txt
/configs/latency_schedule_*.txt

# Per-checkout cluster geometry (answered at ./reproduce.py prereq).
/cluster.json
